import styled from 'styled-components'
import { space, layout, typography, border, flexbox} from 'styled-system'

const Box = styled('div')(space, layout, typography, border, flexbox);
  


export default Box;